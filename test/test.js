const slenzycode = require("slenzycode /* Buraya kendi modül adınızı giriniz package.json name'in karşısına yazdığınız*/");

slenzycode.gpt("sa").then(response => {
    console.log(response);
});
